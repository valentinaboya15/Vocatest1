function comprobar() {
         var pulsado = false;//variable de comprobación
         var opciones = document.formulario.preg1; //array de elementos
         
         var elegido = -1; //número del elemento elegido en el array
         for (i=0;i<opciones.length;i++) { //bucle de comprobación
               if (opciones[i].checked == true) {
               pulsado = true 
               elegido = i //número del elemento elegido en el array
               }
             }
         if (pulsado == true) { //mensaje de formulario válido
            miOpcion = opciones[elegido].value
            }
         else { //mensaje de formulario no válido.
            alert("no has elegido ninguna opción en la PREGUNTA 1\n Elige una opción para que el formulario pueda ser enviado")
            return false //el formulario no se envía.
            }
            comprobar2();
            return false //el formulario no se envía.
           
         }
function comprobar2() {
         var pulsado = false;//variable de comprobación
         var opciones2 = document.formulario.preg2; //array de elementos
         
         var elegido = -1; //número del elemento elegido en el array
         for (i=0;i<opciones2.length;i++) { //bucle de comprobación
               if (opciones2[i].checked == true) {
               pulsado = true 
               elegido = i //número del elemento elegido en el array
               }
             }
         if (pulsado == true) { //mensaje de formulario válido
            miOpcion = opciones2[elegido].value
            
            }
         else { //mensaje de formulario no válido.
            alert("no has elegido ninguna opción en la PREGUNTA 2\n Elige una opción para que el formulario pueda ser enviado")
            return false //el formulario no se envía.
            }
            comprobar3();
            return false //el formulario no se envía.
         } 
function comprobar3() {
         var pulsado = false;//variable de comprobación
         var opciones3 = document.formulario.preg3; //array de elementos
         
         var elegido = -1; //número del elemento elegido en el array
         for (i=0;i<opciones3.length;i++) { //bucle de comprobación
               if (opciones3[i].checked == true) {
               pulsado = true 
               elegido = i //número del elemento elegido en el array
               }
             }
         if (pulsado == true) { //mensaje de formulario válido
            miOpcion = opciones3[elegido].value
            
            }
         else { //mensaje de formulario no válido.
            alert("no has elegido ninguna opción en la PREGUNTA 3\n Elige una opción para que el formulario pueda ser enviado")
            return false //el formulario no se envía.
            }
            comprobar4();
            return false //el formulario no se envía.
         }  
         function comprobar4() {
         var pulsado = false;//variable de comprobación
         var opciones4 = document.formulario.preg4; //array de elementos
         
         var elegido = -1; //número del elemento elegido en el array
         for (i=0;i<opciones4.length;i++) { //bucle de comprobación
               if (opciones4[i].checked == true) {
               pulsado = true 
               elegido = i //número del elemento elegido en el array
               }
             }
         if (pulsado == true) { //mensaje de formulario válido
            miOpcion = opciones4[elegido].value
            
            }
         else { //mensaje de formulario no válido.
            alert("no has elegido ninguna opción en la PREGUNTA 4\n Elige una opción para que el formulario pueda ser enviado")
            return false //el formulario no se envía. 
         }
         comprobar5();
            return false //el formulario no se envía.
        }   
        function comprobar5() {
         var pulsado = false;//variable de comprobación
         var opciones5 = document.formulario.preg5; //array de elementos
         
         var elegido = -1; //número del elemento elegido en el array
         for (i=0;i<opciones5.length;i++) { //bucle de comprobación
               if (opciones5[i].checked == true) {
               pulsado = true 
               elegido = i //número del elemento elegido en el array
               }
             }
         if (pulsado == true) { //mensaje de formulario válido
            miOpcion = opciones5[elegido].value
            
            }
         else { //mensaje de formulario no válido.
            alert("no has elegido ninguna opción en la PREGUNTA 5\n Elige una opción para que el formulario pueda ser enviado")
            return false //el formulario no se envía. 
         }
         comprobar6();
            return false //el formulario no se envía.
        }  
        function comprobar6() {
         var pulsado = false;//variable de comprobación
         var opciones6 = document.formulario.preg6; //array de elementos
         
         var elegido = -1; //número del elemento elegido en el array
         for (i=0;i<opciones6.length;i++) { //bucle de comprobación
               if (opciones6[i].checked == true) {
               pulsado = true 
               elegido = i //número del elemento elegido en el array
               }
             }
         if (pulsado == true) { //mensaje de formulario válido
            miOpcion = opciones6[elegido].value
            
            }
         else { //mensaje de formulario no válido.
            alert("no has elegido ninguna opción en la PREGUNTA 6\n Elige una opción para que el formulario pueda ser enviado")
            return false //el formulario no se envía. 
         }
         comprobar7();
            return false //el formulario no se envía.
        } 
        function comprobar7() {
         var pulsado = false;//variable de comprobación
         var opciones7 = document.formulario.preg7; //array de elementos
         
         var elegido = -1; //número del elemento elegido en el array
         for (i=0;i<opciones7.length;i++) { //bucle de comprobación
               if (opciones7[i].checked == true) {
               pulsado = true 
               elegido = i //número del elemento elegido en el array
               }
             }
         if (pulsado == true) { //mensaje de formulario válido
            miOpcion = opciones7[elegido].value
            
            }
         else { //mensaje de formulario no válido.
            alert("no has elegido ninguna opción en la PREGUNTA 7\n Elige una opción para que el formulario pueda ser enviado")
            return false //el formulario no se envía. 
         }
         comprobar8();
            return false //el formulario no se envía.
        } 
        function comprobar8() {
         var pulsado = false;//variable de comprobación
         var opciones8 = document.formulario.preg8; //array de elementos
         
         var elegido = -1; //número del elemento elegido en el array
         for (i=0;i<opciones8.length;i++) { //bucle de comprobación
               if (opciones8[i].checked == true) {
               pulsado = true 
               elegido = i //número del elemento elegido en el array
               }
             }
         if (pulsado == true) { //mensaje de formulario válido
            miOpcion = opciones8[elegido].value
            
            }
         else { //mensaje de formulario no válido.
            alert("no has elegido ninguna opción en la PREGUNTA 8\n Elige una opción para que el formulario pueda ser enviado")
            return false //el formulario no se envía. 
         }
         comprobar9();
            return false //el formulario no se envía.
        } 
        function comprobar9() {
         var pulsado = false;//variable de comprobación
         var opciones9 = document.formulario.preg9; //array de elementos
         
         var elegido = -1; //número del elemento elegido en el array
         for (i=0;i<opciones9.length;i++) { //bucle de comprobación
               if (opciones9[i].checked == true) {
               pulsado = true 
               elegido = i //número del elemento elegido en el array
               }
             }
         if (pulsado == true) { //mensaje de formulario válido
            miOpcion = opciones9[elegido].value
            
            }
         else { //mensaje de formulario no válido.
            alert("no has elegido ninguna opción en la PREGUNTA 9\n Elige una opción para que el formulario pueda ser enviado")
            return false //el formulario no se envía. 
         }
         comprobar10();
            return false //el formulario no se envía.
        } 
        function comprobar10() {
         var pulsado = false;//variable de comprobación
         var opciones10 = document.formulario.preg10; //array de elementos
         
         var elegido = -1; //número del elemento elegido en el array
         for (i=0;i<opciones10.length;i++) { //bucle de comprobación
               if (opciones10[i].checked == true) {
               pulsado = true 
               elegido = i //número del elemento elegido en el array
               }
             }
         if (pulsado == true) { //mensaje de formulario válido
            miOpcion = opciones10[elegido].value
            
            }
         else { //mensaje de formulario no válido.
            alert("no has elegido ninguna opción en la PREGUNTA 10\n Elige una opción para que el formulario pueda ser enviado")
            return false //el formulario no se envía. 
         }
         comprobar11();
            return false //el formulario no se envía.
        } 
        function comprobar11() {
         var pulsado = false;//variable de comprobación
         var opciones11 = document.formulario.preg11; //array de elementos
         
         var elegido = -1; //número del elemento elegido en el array
         for (i=0;i<opciones11.length;i++) { //bucle de comprobación
               if (opciones11[i].checked == true) {
               pulsado = true 
               elegido = i //número del elemento elegido en el array
               }
             }
         if (pulsado == true) { //mensaje de formulario válido
            miOpcion = opciones11[elegido].value
            
            }
         else { //mensaje de formulario no válido.
            alert("no has elegido ninguna opción en la PREGUNTA 11\n Elige una opción para que el formulario pueda ser enviado")
            return false //el formulario no se envía. 
         }
         comprobar12();
            return false //el formulario no se envía.
        } 
        function comprobar12() {
         var pulsado = false;//variable de comprobación
         var opciones12 = document.formulario.preg12; //array de elementos
         
         var elegido = -1; //número del elemento elegido en el array
         for (i=0;i<opciones12.length;i++) { //bucle de comprobación
               if (opciones12[i].checked == true) {
               pulsado = true 
               elegido = i //número del elemento elegido en el array
               }
             }
         if (pulsado == true) { //mensaje de formulario válido
            miOpcion = opciones12[elegido].value
            
            }
         else { //mensaje de formulario no válido.
            alert("no has elegido ninguna opción en la PREGUNTA 12\n Elige una opción para que el formulario pueda ser enviado")
            return false //el formulario no se envía. 
         }
         comprobar13();
            return false //el formulario no se envía.
        } 
        function comprobar13() {
         var pulsado = false;//variable de comprobación
         var opciones13 = document.formulario.preg13; //array de elementos
         
         var elegido = -1; //número del elemento elegido en el array
         for (i=0;i<opciones13.length;i++) { //bucle de comprobación
               if (opciones13[i].checked == true) {
               pulsado = true 
               elegido = i //número del elemento elegido en el array
               }
             }
         if (pulsado == true) { //mensaje de formulario válido
            miOpcion = opciones13[elegido].value
            
            }
         else { //mensaje de formulario no válido.
            alert("no has elegido ninguna opción en la PREGUNTA 13\n Elige una opción para que el formulario pueda ser enviado")
            return false //el formulario no se envía. 
         }
         comprobar14();
            return false //el formulario no se envía.
        } 
        function comprobar14() {
         var pulsado = false;//variable de comprobación
         var opciones14 = document.formulario.preg14; //array de elementos
         
         var elegido = -1; //número del elemento elegido en el array
         for (i=0;i<opciones14.length;i++) { //bucle de comprobación
               if (opciones14[i].checked == true) {
               pulsado = true 
               elegido = i //número del elemento elegido en el array
               }
             }
         if (pulsado == true) { //mensaje de formulario válido
            miOpcion = opciones14[elegido].value
            
            }
         else { //mensaje de formulario no válido.
            alert("no has elegido ninguna opción en la PREGUNTA 14\n Elige una opción para que el formulario pueda ser enviado")
            return false //el formulario no se envía. 
         }
         comprobar15();
            return false //el formulario no se envía.
        } 
        function comprobar15() {
         var pulsado = false;//variable de comprobación
         var opciones15 = document.formulario.preg15; //array de elementos
         
         var elegido = -1; //número del elemento elegido en el array
         for (i=0;i<opciones15.length;i++) { //bucle de comprobación
               if (opciones15[i].checked == true) {
               pulsado = true 
               elegido = i //número del elemento elegido en el array
               }
             }
         if (pulsado == true) { //mensaje de formulario válido
            miOpcion = opciones15[elegido].value
            
            }
         else { //mensaje de formulario no válido.
            alert("no has elegido ninguna opción en la PREGUNTA 15\n Elige una opción para que el formulario pueda ser enviado")
            return false //el formulario no se envía. 
         }
         validar();
         return false //el formulario no se envía.
        } 
        function validar() {
         //obteniendo el valor que se puso en campo text del formulario
         miCampoTexto = document.getElementById("userName").value;
         //la condición
         if (miCampoTexto.length == 0) {
             alert("Por Favor,escribir tu nombre completo")
             return false;
         }
         return true;
     }
      